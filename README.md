# TEST

A starting point. Nothing is built here yet.

This repository was initialized with no particular language or framework
chosen, so it deliberately contains no build tooling, dependency manifest,
or project structure. Those decisions are still open.

## Getting started

Clone the repository and start adding code:

```sh
git clone https://github.com/mustafaessam4030-lang/TEST.git
cd TEST
```

When the direction is settled, replace this file with a real description of
what the project does and how to run it.

## Next steps

- Decide on a language and toolchain
- Add the corresponding manifest (`pyproject.toml`, `package.json`, etc.)
- Extend `.gitignore` with the ignore rules that toolchain needs
- Replace this README
